'use strict';

import { Rate, RateUtil } from './models/rate.js';
import { Position, PositionUtil } from './models/position.js';

export class Logic {
    static direction(rate = new Rate(), positions = [new Position()]) {

    }
}